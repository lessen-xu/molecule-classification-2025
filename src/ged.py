import numpy as np
# Optional: import networkx as nx

def compute_ged(g1, g2):
    """
    Calculates Graph Edit Distance (GED) between two molecules.
    
    Args:
        g1, g2 (MoleculeGraph): The two graphs to compare.
    Returns:
        float: The distance cost.
    """
    # TODO: Role C Implementation
    # Define costs:
    # - Node Substitution (e.g. C -> O) cost = ?
    # - Edge Deletion/Insertion cost = ?
    # - Node Deletion/Insertion cost = ?
    
    return 0.0